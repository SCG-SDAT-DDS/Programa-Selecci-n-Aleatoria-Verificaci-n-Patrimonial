@model List<Transparencia.Models.CampoCatalogo>

@{

}

    <div id="Campos">

        @if (Model.Count() > 0)
        {
        <div class="form-row">
            @foreach (var item in Model)
            {
                @Html.Raw(item.InputHtmlEdit)

            }
        </div>
        }
    </div>