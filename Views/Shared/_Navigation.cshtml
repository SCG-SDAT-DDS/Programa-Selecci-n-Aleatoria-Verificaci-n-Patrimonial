<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <img src="~/Content/images/LOGO-PROVEP.svg" title="PROGRAMA DE SELECCIÓN ALEATORIA PARA LA VERIFICACIÓN PATRIMONIAL" class="d-none d-sm-block m-auto w-75 pb-3 pt-3">


                    <ul class="dropdown-menu animated fadeInRight m-t-xs" style="position: absolute; top: 91px; left: 0px; will-change: top, left;">
                        @*<li><a class="dropdown-item" href="@Url.Action("Profile", "AppViews")">Profile</a></li>
            <li><a class="dropdown-item" href="@Url.Action("Contacts", "AppViews")">Contacts</a></li>
            <li><a class="dropdown-item" href="@Url.Action("Inbox", "Mailbox")">Mailbox</a></li>*@
                        <li class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="@Url.Action("Login", "Pages")">Logout</a></li>
                    </ul>
                </div>
                @*<div class="logo-element">
                <img alt="image" src="~/Imagenes/LogoTransparncia.png" />
            </div>*@
            </li>

            @{
                var Enlace = HMTLHelperExtensions.GetRoles(User.Identity.Name, "Enlace");
                var UnidadAdministrativa = HMTLHelperExtensions.GetRoles(User.Identity.Name, "Unidad Administrativa");

            }

            @if (UnidadAdministrativa)
            {
                @Html.Partial("Menu/_UnidadAdministrativa")
            }
            else if (Enlace)
            {
                @Html.Partial("Menu/_Enlace")
            }
            else
            {
                @Html.Partial("Menu/_Administrador")
            }
        </ul>
    </div>
</nav>